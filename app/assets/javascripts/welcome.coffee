# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

$ ->
  # $('form.sign-up input[type="submit"]').attr("disabled", true)

  # validEmailPattern = /.+@.+\..+/i

  # $('form.sign-up').on "keyup input paste", ->

  #   userEmail = $('form.sign-up input[type="email"]').val()
  #   console.log userEmail

  #   if userEmail.match validEmailPattern
  #     $('form.sign-up input[type="submit"]').attr("disabled", false)
  #     console.log "valid!"

  # $('form.sign-up').submit ->
  #   $(this, '.welcome-hero h6').addClass('complete')
  #   $('.welcome-hero h6').css({
  #     'bottom': '40px', 
  #     'opacity': 1, 
  #     'transform': 'translateY(-30px)'
  #     })
  return